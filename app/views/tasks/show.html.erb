<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1><%= @task.title%></h1>
  <div>
    <p><%= @task.details %></p>
  </div>

  <% if @task.completed%>
    <input type="checkbox" id= `{task.title}` name="vehicle1" value="Bike" checked>
    <p>Task is completed</p>
  <% elsif%>
    <input type="checkbox" id= `{task.title}` name="vehicle1" value="Bike">
    <p>Task is not completed yet</p>
  <% end %>

  <%= link_to "Edit this task", edit_path(@task)%>

  <%= link_to "Delete this task", destroy_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>

   <%= link_to "Back to list", tasks_path%>
</body>
</html>
