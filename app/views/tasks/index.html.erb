<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>My tasks</h1>

  <% @tasks.each do |task|%>
    <% if task.completed%>
      <input type="checkbox" id= `{task.title}` name="vehicle1" value="Bike" checked>
      <%= link_to task.title, task_path(task)%><br>
    <% elsif %>
      <input type="checkbox" id= `{task.title}` name="vehicle1" value="Bike">
      <%= link_to task.title, task_path(task)%><br>
    <% end %>
  <% end %>

  <%= link_to 'Add a new task', new_path%>
</body>
</html>
